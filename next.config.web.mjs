// next.config.web.mjs - Configuration for regular web builds (Vercel)

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Standard Next.js configuration without WebSpatial
};

export default nextConfig;
